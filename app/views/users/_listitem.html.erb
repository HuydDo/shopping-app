  <!-- <li>
    <%= link_to order.item.name,  item_path(order.item) %>
      Price: $<%=  order.item.price %>
      Qty: <%= order.quantity %> 
      Total: $<%= order.item.price * order.quantity%>
    <%= link_to "Edit", edit_order_path(order) %>
    <%= link_to "Delete", order_path(order), method: :delete, data: { confirm: "Are you sure you want to delete this order?" } %>
     <%# link_to "Delete", person, method: :delete, data: { confirm: "Really?" } %>
    
 </li>
-->

 
<tr>
<!--    <td> <%= link_to order.item.name,  item_path(order.item) %></td>  -->
  <td> <%= order.item.name %></td>
  <td><%=  number_to_currency(order.item.price) %></td>
  <td><%= order.quantity %></td>
  <td> <%= number_to_currency(order.item.price * order.quantity)%></td>
  <td> <%= link_to "Edit", edit_order_path(order) %></td>
  <td><%= link_to "Delete", order_path(order), method: :delete, data: { confirm: "Are you sure you want to delete this order?" } %></td>
</tr>
