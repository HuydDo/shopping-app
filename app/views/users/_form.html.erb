<%= form_with model: @user, local: true do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being save:</h2>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
     </div>
  <% end %>

  <div class="field">
    <%= f.label "Name:" %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Password:" %>
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end  %>
