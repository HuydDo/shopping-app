<!--Subtotal (<%= item_count %> items): <%= number_to_currency(subtotal) %><br />  -->

Subtotal (<%= pluralize(@user.orders.count, "order") %>): <%= number_to_currency(subtotal) %><br />
Subtotal (<%= pluralize(@user.items.count, "items") %>): <%= number_to_currency(subtotal) %><br />
